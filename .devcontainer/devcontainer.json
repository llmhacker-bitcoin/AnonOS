{
  "name": "AnonOS Build Environment",
  "image": "mcr.microsoft.com/devcontainers/base:debian",
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {}
  },
  "runArgs": ["--privileged"],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y live-build debootstrap xorriso isolinux syslinux-efi git curl wget",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.makefile-tools"
      ]
    }
  },
  "remoteUser": "vscode"
}